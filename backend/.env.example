# 视障语义地图后端服务 - 环境变量配置

# 服务器配置
PORT=3000
NODE_ENV=development

# MongoDB 配置
MONGODB_URI=mongodb://localhost:27017/blind_map

# Overpass API 配置（可选，使用默认公共 API）
OVERPASS_API_URL=https://overpass-api.de/api/interpreter
